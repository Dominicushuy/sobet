/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        // Bạn có thể thêm màu tùy chỉnh ở đây
      },
      fontFamily: {
        // Thêm font tùy chỉnh
      },
    },
  },
  plugins: [],
};
